# Required - create a strong key
API_KEY=changeme

# Optional — version string returned by /api/v1/status.
VERSION=1.0.0

# Optional — port the Flask app listens on inside the container, default is 5000.
PORT=5000